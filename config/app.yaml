site:
  posts_per_run: 1
  category_names: ["レビュー"]
  affiliate_disclosure: "当サイトはアフィリエイト広告（Amazonアソシエイト含む）を利用しています。"
  unique_slug_date: true          # ← 重複時に -YYYYMMDD を付与（今回のボトルネック解消）
data_sources:
  rakuten:
    endpoint: "https://app.rakuten.co.jp/services/api/IchibaItem/Search/20220601"
    max_per_seed: 100             # ← 設定は100でもOK。コード側で30件ごと自動ページング
